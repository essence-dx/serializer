{
  "name": "dx-serializer",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/cli"
  ],
  "scripts": {
    "test": "bun test --filter @dx-serializer/*"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^6.0.3"
  }
}
